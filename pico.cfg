# === Interface ===
adapter speed 5000
source [find interface/cmsis-dap.cfg]    ;# Use your CMSIS-DAP probe
transport select swd                     ;# Use SWD interface

# === Target ===
source [find target/rp2350.cfg]         ;# RP2040 / RP2350 target config

# === Reset settings ===
reset_config srst_nogate                ;# Use standard reset
